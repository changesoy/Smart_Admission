# 智慧入学·学区治理一站式可视化门户 — 项目完善规划（Public Beta 版）

> 编制日期: 2026-09-20
> 基于版本: feature/A-map
> 计划周期: 12 周（约 3 个月）
> 总预算: 200 人·小时
> 阶段目标: Smart Admission Public Beta v1.0

---

## 〇、规划定位与本次调整

### 【学期总目标】

在保持当前轻量前端 GIS 架构的前提下，完成核心查询正确性、数据可信化、规则化入学自查、移动端适配、异常处理和公网部署，使项目从"课程功能原型"升级为：

1. 可通过公开 URL 访问；
2. 核心查询流程稳定；
3. 数据来源、年份、状态可追溯；
4. 手机端可以正常使用；
5. 网络/API 出错时有明确降级；
6. 可进行低成本持续维护；
7. 可作为课程答辩成果，同时具备真实 Public Beta 使用价值。

---

## 一、当前项目状态

### 【已经具备的核心能力】

- Leaflet 地图展示；
- Turf 点面判断；
- GeoJSON 学区边界展示；
- 小学 / 初中学区数据；
- 地址点本地检索；
- 天地图 POI 联网搜索；
- 地图点击查询；
- 学校信息展示；
- 政策归档与年度筛选；
- 政策年度对比；
- 入学材料清单；
- FAQ 分类筛选与相关问题跳转；
- 留言表单与本地存储；
- ESLint；
- 数据校验脚本；
- README / CONTRIBUTING / 数据说明等文档；
- 模块化 Service 结构；
- 单个模块失败时的初始化隔离能力。

### 【当前优先问题】

**A. 核心业务逻辑**

- 一个地址可能同时属于一个小学学区和一个初中学区，当前查询逻辑容易只返回第一个命中的学区。
- 地图"图层是否显示"不应影响真实学区归属查询。
- 地图点击查询、地址查询、关键词查询需要统一结果模型。
- 搜索结果应稳定关联到学区、学校、政策和材料。

**B. 工程问题**

- searchService 中部分代码风格与当前 ESLint 规则不一致。
- 联系方式代码存在但页面挂载点不完整。
- README / CONTRIBUTING / 页面提示与当前联网搜索能力存在不同步。
- 核心数据加载方式、错误处理仍有优化空间。

**C. 数据问题**

- 部分学区数据仍是人工近似边界。
- 不同数据文件的年份并不完全一致。
- 部分 sourceUrl / 电话 / 地址仍包含占位内容。
- 数据状态虽然已有 estimated / verified 等概念，但未形成统一发布门禁。

**D. 部署问题**

- 当前仍偏向本地 HTTP Server 运行。
- 部分依赖来自外部 CDN。
- 缺少明确的正式/开发配置区分。
- 缺少 Public Beta 发布流程和 release 检查。
- 缺少简单线上运行监控。

**E. 用户体验问题**

- 移动端仍需要完整真机检查。
- 网络失败、在线 API 失败、无搜索结果等状态需要统一设计。
- 留言"提交成功"目前仅代表保存到 localStorage，容易产生误解。

---

## 二、技术路线与范围边界

### 【本学期保持的总体架构】

```text
浏览器
  │
  ├─ HTML / CSS / Vanilla JS
  ├─ Leaflet
  ├─ Turf
  ├─ data/*.json
  ├─ data/zones.geojson
  └─ 天地图服务（底图 / POI）

静态托管 + HTTPS
  │
  └─ （可选）Backend Lite：feedback / health
```

### 【核心设计原则】

1. **查询事实与地图展示状态分离**
   - 地图图层开关只控制视觉显示；
   - 业务查询始终按照完整学区数据进行。

2. **数据驱动**
   - 政策、材料、规则、学区、学校尽量通过数据配置；
   - JS 负责解释和展示数据，不大量硬编码政策规则。

3. **不做无依据预测**
   - 不计算"录取概率"；
   - 不用自定义分值假装真实录取模型；
   - 自查结果必须能关联政策依据或明确标记为一般提示。

4. **静态优先**
   - 公共只读数据继续使用 JSON/GeoJSON；
   - 没有明确业务价值时不引入数据库复杂度。

5. **可发布优先**
   - 每个新增功能都要考虑手机端、异常状态和上线环境；
   - 第 8 周前完成首个线上 Beta。

6. **可回退**
   - 联网 POI 查询失败时，本地已有查询能力不应被拖垮；
   - 第三方依赖故障时尽量保留核心内容展示。

---

## 三、任务清单（按优先级）

### ━━━ P0 · Public Beta 发布底线（必须完成）━━━

#### [P0-1] 核心查询正确性 + 当前 Bug 修复

- **归属**: A组（地图与查询）为主，全员联调
- **估时**: 14h

**目标**:
把"地址 → 坐标 → 学区 → 学校 → 政策/材料"的核心链路做正确。

**需求**:

- 将单结果 findZoneByPoint 思路调整为多学段结果：
  - primary（小学）
  - middle（初中）
- 支持同一坐标同时返回小学和初中归属；
- 地图点击与地址搜索使用同一个业务查询入口；
- 地图可见图层状态不得影响真实业务查询；
- 图层开关只影响地图显示；
- 搜索结果面板分别展示小学和初中结果；
- 无匹配时返回明确空状态，不自动猜测最近学校；
- 修复 Contact 区域挂载问题；
- 修复 searchService 与 ESLint 的明显冲突；
- 修正文档中与当前在线 POI 能力不一致的描述；
- 检查模块间 callback 和错误处理是否一致。

**建议代码改动**:

- js/mapService.js
- js/searchService.js
- js/main.js
- js/render.js
- js/interactionService.js
- index.html
- README.md
- CONTRIBUTING.md

**验收**:

- 给定一个同时覆盖小学/初中学区的坐标，可同时返回两个学段；
- 隐藏小学图层后查询同一地址，小学业务结果仍存在；
- 地图点击和搜索同一坐标得到一致结果；
- 区域外坐标明确显示"未匹配到当前数据范围内学区"；
- npm run lint 无报错；
- 控制台无核心流程异常。

#### [P0-2] 数据可信化 + 数据版本体系 + Release Validator

- **归属**: 全员，B组负责政策与规则数据，A组负责空间数据
- **估时**: 30h

**目标**:
将数据从"可以展示"提升到"可追溯、可判断状态、可阻止占位数据误发布"。

**第一部分：数据清理**

- 逐个检查：
  - zones.geojson
  - schools.json
  - policies.json
  - materials.json
  - faq.json
  - contacts.json
  - address_points.json
  - keywords_index.json
  - 其他实际加载的数据文件
- 统一年份；
- 删除或明确标注过期数据；
- 替换能够核验的真实来源；
- 对暂时无法获得精确边界的数据继续标注 estimated / manualApproximation；
- 不允许把人工近似 Polygon 描述成官方精确边界；
- 清理"待补""示例""example.gov.cn""000000"等占位值；
- 不确定的数据宁可标记 unknown / pending，不伪造 verified。

**第二部分：统一元信息**

核心业务数据尽量补充：

- effectiveYear
- dataVersion
- sourceName
- sourceUrl
- sourcePublishedAt
- verifiedAt
- dataStatus
- geometryAccuracy（空间数据适用）
- notes（必要时）

**第三部分：增强 validate-data.mjs**

增加检查：

- JSON/GeoJSON 语法；
- ID 唯一性；
- zoneId → schoolId → policyId 关联；
- 所有引用 ID 必须存在；
- Polygon 闭合；
- 坐标范围；
- 经纬度顺序；
- 学段字段合法；
- 年份合法；
- sourceUrl 格式；
- required 元字段存在；
- 生产数据禁止 placeholder；
- 小学学区异常重叠提示；
- 初中学区异常重叠提示；
- Polygon 空洞 / 自相交能检查则检查；
- 未使用/孤立数据提示。

**第四部分：开发检查与发布检查分离**

建议新增:

- `npm run validate:data` — 允许开发中的 pending / estimated 数据，但要求结构合法。
- `npm run check` — lint + validate:data。
- `npm run check:release` — 更严格，禁止正式发布目录中出现约定的占位模式。

建议 placeholder 黑名单:

- example.gov.cn
- 待补
- TODO
- 示例电话
- 000000
- XXXXXXXX
- fake
- mock（正式业务数据中）

**验收**:

- npm run check 通过；
- npm run check:release 通过，或仅存在明确允许的 estimated 空间数据白名单；
- 所有正式展示的政策均能看到来源；
- 页面能够展示"数据年份 / 更新时间 / 数据状态"；
- 学区边界近似时，用户能看到清晰的数据精度说明；
- 不存在把占位 URL 标成 verified 的情况。

#### [P0-3] 入学条件自查 / 情形判断助手

- **归属**: B组（政策与模拟）为主
- **估时**: 24h

**目标**:
保留选题报告中的"模拟/辅助判断"价值，但不输出无真实依据的录取概率。

**功能名称建议**: "入学条件自查" 或 "入学情形判断助手"

**输入建议**:

- 申请学段（小学 / 初中）
- 户籍情况
- 住房情况
- 户籍与住房是否一致
- 是否实际居住
- 居住/证件情况（依据真实政策决定是否保留）

**结果建议**:

- 当前情形分类；
- 需要重点核验的条件；
- 建议准备材料；
- 相关政策条目；
- 需要人工咨询的事项；
- 官方来源链接；
- 数据年份。

**禁止**:

- "85 分"
- "高概率录取"
- "录取成功率 90%"
- 没有真实统计模型支撑的排名或预测。

**数据结构建议**: `data/simulator_rules.json`

每条规则建议包含:

- ruleId
- effectiveYear
- stage
- conditions
- resultType
- requiredMaterialIds
- warnings
- policyIds
- sourceUrl
- dataStatus

**实现**:

- 创建/调整 js/simulatorService.js；
- 表单根据规则数据渲染；
- 匹配规则与 UI 解耦；
- 输出材料与政策可直接跳转；
- 规则无法覆盖时明确提示"需要人工确认"，不要强行给结论；
- 在 main.js / dataService.js / config.js 正确接入。

**验收**:

- 不同用户情形可匹配不同规则；
- 结果能够关联政策与材料；
- 规则更换年度时尽量不修改核心 JS；
- 未命中规则时有安全的 fallback；
- 页面不出现无依据"录取概率"。

#### [P0-4] Public Beta 部署工程

- **归属**: A组 + C组，全员参与发布
- **估时**: 18h

**目标**:
让项目不再只存在于开发电脑中，形成真实可访问的线上 Beta。

**需求**:

- 选择静态托管方式；
- HTTPS；
- 配置正式环境与开发环境差异；
- 固定第三方依赖版本；
- Leaflet / Turf / Bootstrap / Icons 等关键依赖优先本地化或由可控资源托管；
- 首页图片等关键资源避免依赖不稳定临时链接；
- 添加 404 页面或静态托管平台 fallback；
- 设置合理 Cache-Control；
- 数据文件更新时避免旧缓存长期存在；
- 启用压缩（由托管平台/Nginx提供即可）；
- 增加版本号 / build 信息 / 数据更新时间；
- 保留本地快速启动方式；
- 编写 RELEASE.md 或 README 发布章节；
- 首次部署不晚于第 8 周；
- 第 12 周发布 v1.0-beta。

**可选**:

- 简单 uptime 检查；
- 简单前端错误统计（不引入复杂平台也可先记录）。

**验收**:

- 有公开可访问 URL；
- 手机网络环境可以正常访问；
- HTTPS 正常；
- 刷新/直接访问主要 URL 不出现异常；
- 外部 CDN 暂时不可用时，不影响核心静态依赖；
- 数据更新后可确认线上版本已经刷新；
- README 中包含线上地址和本地运行方式。

#### [P0-5] 答辩材料 + Release 展示

- **归属**: 全员
- **估时**: 20h

**目标**:
将"Public Beta 已上线"本身变成答辩亮点。

**内容**:

- PPT 5-10 页：
  - 背景与痛点
  - 当前架构
  - 地址到学区的 GIS 查询链路
  - 数据可信与数据版本设计
  - 入学条件自查
  - Public Beta 部署
  - 核心测试与数据检查
  - 后续路线
- 5 分钟演示视频；
- 在线 URL / 二维码；
- 脱网或本地运行备用方案；
- 至少 3 次演练；
- README 最终更新；
- 版本 tag / release note（如团队流程允许）；
- 准备"数据边界和项目边界"说明。

**验收**:

- 在线演示可以完成核心流程；
- 本地备用演示可用；
- PPT 与当前实际功能一致；
- 不把未实现功能写成已实现；
- 演示流程控制在规定时间内。

### ━━━ P1 · 高价值生产化任务（原则上完成）━━━

#### [P1-1] 移动端深度适配 + 基础无障碍

- **归属**: C组（互动与内容）
- **估时**: 18h

**目标**:
优先保证手机端真正可用，同时保留成本较低的基础无障碍能力。

**移动端**:

- 992 / 768 / 576 px 断点检查；
- 地图高度适配；
- 搜索框和筛选区窄屏布局；
- 搜索结果面板避免遮挡地图；
- 结果出现后合理滚动；
- 政策卡片单列/双列布局；
- 材料 Tab 窄屏可滚动；
- FAQ 标签换行；
- 表单移动端布局；
- 触摸点击区域不小于合理尺寸；
- 避免固定宽度和横向溢出。

**基础无障碍**:

- 图片 alt；
- 表单 label / aria-label；
- 动态结果区域 aria-live；
- :focus-visible；
- 键盘 Tab 可达；
- 不只用颜色区分小学/初中；
- 表单错误与输入项关联。

**测试尺寸**:

- 小屏手机；
- 主流手机；
- 平板；
- 桌面。

**验收**:

- 主流程可单手完成；
- 无明显横向滚动；
- 文字和按钮不截断；
- 地图点击查询在触屏可用；
- 键盘可访问核心功能。

#### [P1-2] 地图 / 搜索生产容错

- **归属**: A组（地图与查询）
- **估时**: 14h

**目标**:
在线 POI 或网络异常时，页面不"静默失败"。

**需求**:

- fetch 后检查 HTTP 状态；
- 增加合理超时；
- AbortController 继续保留，避免旧请求覆盖新请求；
- token 失效错误处理；
- 异常 JSON / 空响应处理；
- 在线搜索失败时提示并保留本地查询能力；
- 搜索中 loading 状态；
- 无结果状态；
- 结果超出支持区域提示；
- 多个同名 POI 有可区分信息；
- 去除明显重复结果；
- 搜索结果点击后地图定位；
- 网络恢复后可重新查询；
- 地图瓦片失败不阻塞文本信息；
- 可考虑短时间内同查询结果缓存。

**验收**:

- 模拟 404 / 500 / 超时均有友好反馈；
- 天地图搜索不可用时，本地地址/关键词功能仍能使用；
- 快速连续搜索不会显示旧请求结果；
- 范围外 POI 不被错误判定为当前服务区域。

#### [P1-3] CI + 数据质量门禁

- **归属**: 全员
- **估时**: 10h

**目标**:
让最基本的质量检查从"记得手动运行"变成"每次提交都能执行"。

**建议**:

- GitHub Actions（或当前代码托管支持的 CI）；
- Node 环境固定；
- npm ci；
- npm run lint；
- npm run validate:data；
- 对 release/main 分支运行 npm run check:release；
- CI 失败则不认为达到发布条件。

**可选**:

- 自动检查内部文件路径；
- 简单链接检查；
- 生成 validator 报告 artifact（时间足够再做）。

**验收**:

- 新提交可自动执行检查；
- 故意加入重复 ID / 非闭合 Polygon / lint error 时 CI 能失败；
- 正常代码 CI 通过。

#### [P1-4] 核心回归测试（轻量）

- **归属**: A组为主，全员维护
- **估时**: 10h

**原则**:
不追求测试覆盖率数字。只保护"错误后果最大"的核心链路。

**最低测试集**:

1. 坐标 A → 小学 X + 初中 Y；
2. 坐标 B → 小学 C + 初中 D；
3. 区域外坐标 → no match；
4. 隐藏小学图层 → 查询仍返回真实小学归属；
5. zoneId 引用的 school 必须存在；
6. school / policy / material 关键关联不为空；
7. 搜索结果坐标格式正确；
8. 规则助手给定固定输入 → 固定规则结果。

**实现方式**:

- 优先使用简单 Node 测试脚本；
- GIS 测试可直接调用 Turf；
- 如果团队已经熟悉 Playwright，可只增加 1 条核心 smoke flow；
- 不为了测试框架本身消耗大量时间。

**人工回归**:

- Chrome；
- Edge；
- 一个移动端浏览器；
- Firefox 做一次主要功能抽查。

**验收**:

- 核心固定测试可一条命令运行；
- 修地图/UI 时能检测关键业务回归；
- 不要求"所有 Bug 清零"，但不允许已知 Blocker / 核心 Critical Bug。

#### [P1-5] 政策 / 材料 / FAQ 实用性优化 + 打印

- **归属**: B组 + C组
- **估时**: 10h

**需求**:

- 政策详情显示：
  - 年份
  - 发布/生效信息（有则显示）
  - 数据来源
  - 官方原文入口
- 材料清单和自查结果互相跳转；
- FAQ 可从相关政策/材料入口跳转；
- 材料清单保留勾选进度；
- 增加打印友好样式：
  - 材料清单
  - 自查结果（可选）
  - 政策摘要（可选）
- 打印隐藏导航、地图和不必要交互。

**验收**:

- 用户从自查结果可以快速找到所需材料和政策；
- Ctrl+P / 系统打印预览可读；
- A4 不出现大量空白页；
- 数据来源清晰。

### ━━━ P2 · 有余力完成（可砍，不阻塞 Beta）━━━

#### [P2-1] 地图/加载视觉完善

- **归属**: A组 + C组
- **估时**: 8h

**可选内容**:

- 矢量底图 / 影像底图切换；
- 更统一的 loading 状态；
- 简化骨架屏（不做复杂 shimmer 也可以）；
- 地图控件移动端布局；
- 学区 hover/click 视觉优化；
- Marker 状态统一；
- 空状态图标和提示统一。

**验收**:

- 不影响核心地图查询；
- 视觉调整不会破坏移动端；
- 图层切换只改变展示，不改变查询结果。

#### [P2-2] Backend Lite：真实反馈 / 健康检查

- **归属**: C组或熟悉后端成员
- **估时**: 6h

本任务是"可选实验"，禁止扩成完整账号系统。

**推荐最小范围**:

- GET /api/health
- POST /api/feedback

feedback 建议字段:

- category
- content
- page/context
- createdAt

**限制**:

- 不做注册；
- 不做登录；
- 不做 JWT；
- 不做密码；
- 不做复杂管理后台；
- 不把所有 JSON 导入数据库。

**如果未做 Backend Lite**:

- 将现有"留言已提交"改成不会误导用户的表述；
- 或暂时改为"本机保存的意见草稿/体验记录"；
- 不声称反馈已经提交给运营方。

**技术选型**:

- A. FastAPI + SQLite
- B. Express + SQLite

以团队最熟悉方案为准。

**验收**:

- 若实现：反馈真实到达服务端并可以查看；
- 若不实现：前端不产生"已发送到平台"的误导。

#### [P2-3] 发布文档 + 数据维护手册

- **归属**: 全员
- **估时**: 8h

**新增/完善**:

- README.md
- CONTRIBUTING.md
- RELEASE.md（建议）
- data-schema.md
- DATA_MAINTENANCE.md（建议）

DATA_MAINTENANCE.md 至少说明:

- 新年度政策如何录入；
- 学区边界如何更新；
- 学校关联如何修改；
- sourceUrl 如何填写；
- dataStatus 如何选择；
- verifiedAt 如何记录；
- 修改后要运行哪些命令；
- 什么时候允许发布 estimated 数据；
- 如何回滚上一版数据。

RELEASE.md 至少说明:

- npm install / npm ci；
- npm run check；
- npm run check:release；
- 本地预览；
- 正式部署；
- 发布后 smoke check；
- 数据缓存刷新方法；
- 回滚方法。

**验收**:

- 新成员不问原作者，也能完成一次数据更新并部署测试版；
- README 与实际代码能力一致。

---

## 四、本学期不再作为高优先级的功能

以下功能保留为未来需求池，不进入本次 200h 的核心承诺：

1. **"好差评"本地统计** — localStorage 无法形成真实"总体好评率"，展示意义有限。只有真实后端统计后再恢复该需求。
2. **搜索历史** — 使用价值存在，但不影响核心查询。
3. **政策收藏** — 没有账号体系时仅是当前浏览器本地状态。
4. **全站搜索聚合** — 当前单页模块已有清晰入口，优先级低于真实地址查询。
5. **面包屑 / 网站地图** — 当前单页结构收益较低。
6. **复杂骨架屏** — 可以保留简单 loading，优先处理真实错误状态。
7. **适老版 + Web Speech 语音朗读** — 有价值但工作量相对大，本学期先完成基础可访问性。
8. **学区对比** — 非核心任务。
9. **学位预警 Mock** — 没有可信招生人数和适龄人口数据时容易制造虚假精确感。
10. **完整家长账号体系** — 当前核心查询不需要登录；账号会引入密码、找回、权限和用户数据成本。
11. **所有公开 JSON 数据数据库化** — 静态公开数据目前更简单可靠，数据库化应由真实运营需求驱动。

---

## 五、200 人·小时工时预算

| 工作包 | 工时 |
| --- | --- |
| P0-1 核心查询正确性 + 当前 Bug 修复 | 14h |
| P0-2 数据可信化 + 数据版本 + Release Validator | 30h |
| P0-3 入学条件自查 / 情形判断助手 | 24h |
| P0-4 Public Beta 部署工程 | 18h |
| P0-5 答辩材料 + Release 展示 | 20h |
| **P0 小计** | **106h** |
| P1-1 移动端深度适配 + 基础无障碍 | 18h |
| P1-2 地图 / 搜索生产容错 | 14h |
| P1-3 CI + 数据质量门禁 | 10h |
| P1-4 核心回归测试（轻量） | 10h |
| P1-5 政策 / 材料 / FAQ + 打印 | 10h |
| **P1 小计** | **62h** |
| P2-1 地图/加载视觉完善 | 8h |
| P2-2 Backend Lite | 6h |
| P2-3 发布文档 + 数据维护手册 | 8h |
| **P2 小计** | **22h** |
| 缓冲工时 | 10h |
| **总计** | **200h** |

### 【预算原则】

- 不允许 P2 挤占 P0。
- 数据可信和核心查询出现延期时，优先砍 P2。
- 测试保持轻量，不扩张到追求覆盖率。
- Backend Lite 只允许 6h timebox，超过立即停止扩展。
- 第 8 周必须优先得到线上 Beta，不以"功能还没全部做完"为理由推迟上线。

---

## 六、12 周执行计划

### 第一阶段：第 1-4 周

> 目标：先把"查得对"和"数据敢展示"解决
> 预算：58h

**Week 1-2（24h）**

- P0-1 核心查询正确性 + 当前 Bug 修复：14h
- P0-2 数据可信化第一轮：10h

重点:

- 双学段查询；
- 图层显示与业务查询解耦；
- 统一地图点击/搜索查询链路；
- contacts / lint / 文档不一致修复；
- 建立数据问题清单；
- 开始清理 2025/2026 数据年份和 placeholder。

**Week 3-4（34h）**

- P0-2 数据可信化第二轮：20h
- P1-3 CI + 数据质量门禁：10h
- P1-4 核心测试第一批：4h

重点:

- source / version / status 字段统一；
- validate:data 增强；
- check:release；
- CI；
- 固定坐标回归样本。

**里程碑 M1：第 4 周末**，必须达到:

- 同一地址可以正确返回小学 + 初中；
- 图层开关不影响业务归属；
- npm run lint 通过；
- npm run validate:data 通过；
- release placeholder 检查已经建立；
- 核心数据均有明确"年份/状态/来源策略"；
- 至少 4 条核心查询回归样本可自动执行。

### 第二阶段：第 5-8 周

> 目标：完成自查助手、移动端和首个线上 Beta
> 预算：74h

**Week 5-6（32h）**

- P0-3 入学条件自查 / 情形判断助手：24h
- P1-5 政策/材料/FAQ 实用性优化：8h

重点:

- simulator_rules 数据模型；
- 规则匹配；
- 结果关联政策与材料；
- 不做录取概率；
- 政策来源展示；
- 材料入口联动。

**Week 7（24h）**

- P1-2 地图 / 搜索生产容错：14h
- P1-1 移动端适配第一轮：10h

重点:

- HTTP/超时/token/空响应处理；
- 本地查询 fallback；
- 多 POI 体验；
- 主要手机断点；
- 触摸地图查询。

**Week 8（18h）**

- P1-1 移动端适配第二轮：8h
- P0-4 Public Beta 部署第一阶段：10h

重点:

- 真机修复；
- HTTPS；
- 静态托管；
- 关键依赖稳定化；
- 首次公开 Beta 部署；
- 线上 smoke check。

**里程碑 M2：第 8 周末**，必须达到:

- 入学自查完整可用；
- 手机端核心流程可完成；
- 在线 POI 异常不会拖垮网站；
- 有真实 Public Beta URL；
- 地址 → 学区 → 学校 → 政策/材料 → 自查 主流程在线可跑通。

### 第三阶段：第 9-10 周

> 目标：上线后修复、功能冻结、完成维护能力
> 预算：38h

**Week 9（18h）**

- P0-4 Public Beta 部署第二阶段：8h
- P2-2 Backend Lite：6h
- P2-1 视觉完善：4h

说明: Backend Lite 如果 6h 内无法形成完整最小闭环，立即停止，将反馈功能调整为不误导用户的本地体验记录。

**Week 10（20h）**

- P2-1 视觉完善：4h
- P2-3 发布文档 + 数据维护手册：8h
- P1-5 政策/材料/FAQ + 打印收尾：2h
- P1-4 核心回归测试收尾：6h

重点:

- 功能冻结；
- 数据维护文档；
- release 流程；
- 关键测试；
- 打印；
- 修复线上真实环境暴露的问题。

**里程碑 M3：第 10 周末**，必须达到:

- 功能 Freeze；
- 无已知 Blocker；
- 无已知核心业务 Critical Bug；
- npm run check / check:release 通过；
- 更新数据和发布网站均有文档；
- Beta 已经过至少 2 周线上环境验证。

### 第四阶段：第 11-12 周

> 目标：稳定 Release + 答辩
> 预算：30h

**Week 11（16h）**

- P0-5 答辩材料：12h
- 缓冲：4h

工作: PPT；演示视频；二维码；线上和本地两套演示；根据演练修复最后问题。

**Week 12（14h）**

- P0-5 答辩材料收尾：8h
- 缓冲：6h

工作: 最终数据检查；release check；README 最终同步；v1.0-beta 发布；至少 3 次完整演练；最终版本冻结。

**里程碑 M4：第 12 周末**，最终交付:

- Public Beta 在线地址；
- v1.0-beta 可复现版本；
- README / RELEASE / 数据维护文档；
- 数据质量检查通过；
- 核心回归测试通过；
- 演示视频；
- 答辩 PPT；
- 本地备用包/运行方案。

---

## 七、核心验收标准

### 【业务正确性】

- 一个地址可以同时返回小学和初中结果；
- 业务查询不受图层显示状态影响；
- 地图点击和地址搜索结果一致；
- 区域外地址不会被强行分配学校；
- 自查助手不生成无依据录取概率。

### 【数据】

- 核心展示数据有年份；
- 核心展示数据有来源策略；
- 正式发布不包含禁止占位符；
- 近似学区边界明确标记精度；
- 关键 ID 引用完整；
- npm run check:release 通过。

### 【搜索】

- 在线 POI 成功时可定位并查询；
- 在线 POI 失败时有提示；
- 本地查询不被在线错误拖垮；
- 快速输入不会被旧请求覆盖；
- 超范围结果有明确提示。

### 【移动端】

- 小屏手机无横向溢出；
- 地图、搜索、自查、材料均可操作；
- 点击区域足够；
- 结果可读；
- 关键流程无需桌面端才能完成。

### 【部署】

- HTTPS；
- 线上 URL 稳定；
- 核心资源可加载；
- 数据更新机制明确；
- 发布可回滚；
- README 有线上地址和本地运行方式。

### 【测试】

- 至少覆盖固定坐标双学段查询；
- 至少覆盖区域外查询；
- 至少覆盖图层隐藏不影响业务；
- 至少覆盖规则助手固定输入；
- 不要求追求高覆盖率百分比。

### 【质量门槛】

**允许**:

- 少量已记录的普通 UI Bug；
- 不影响结果的视觉问题；
- P2 未完成功能。

**不允许**:

- Blocker；
- 已知核心业务 Critical Bug；
- 错误学区结果仍被当作正确结果展示；
- 正式数据含明显 placeholder；
- 页面把本地保存反馈说成已提交到服务器；
- README 宣称不存在的功能。

---

## 八、角色建议与协作

### 【A组 · 地图与查询】

**主责**:

- P0-1 核心查询；
- P1-2 搜索容错；
- GIS 核心测试；
- P0-4 部署协作；
- 空间数据检查。

本学期重点不是继续增加复杂地图功能，而是保证"坐标 → 学区"的准确性和稳定性。

**交付物**:

- 多学段查询 API；
- 查询/显示解耦；
- 搜索错误状态；
- GIS 回归样例。

### 【B组 · 政策与模拟】

**主责**:

- P0-2 政策/材料数据清理；
- P0-3 入学条件自查；
- P1-5 政策/材料/FAQ 联动；
- 数据来源与年份管理。

本学期重点: 把"模拟器"从展示分数改为规则引擎，并让输出真正引用政策和材料。

**交付物**:

- simulator_rules.json；
- simulatorService；
- 政策/材料关联；
- 规则数据维护说明。

### 【C组 · 互动与内容】

**主责**:

- P1-1 移动端；
- 基础无障碍；
- 错误/空状态；
- 打印；
- 可选 Backend Lite；
- 发布文档协作。

本学期重点: 保证真实用户尤其是手机端用户能够完成流程，避免"桌面端看起来很好、手机端不能用"。

**交付物**:

- 移动端样式；
- 统一反馈状态；
- 打印；
- feedback（如果实现）。

### 【全员共同任务】

- 数据核验；
- CI；
- Beta 上线；
- 线上 smoke check；
- 版本发布；
- 答辩；
- 文档。

---

## 九、预计文件改动清单

**核心查询**:

- js/mapService.js
- js/searchService.js
- js/main.js
- js/render.js
- index.html

**数据**:

- data/zones.geojson
- data/schools.json
- data/policies.json
- data/materials.json
- data/faq.json
- data/contacts.json
- data/address_points.json
- data/keywords_index.json
- data/simulator_rules.json（新增/重构）

**自查**:

- js/simulatorService.js（新增/重构）
- css/style.css
- index.html
- js/dataService.js
- js/config.js
- js/main.js

**数据质量**:

- scripts/validate-data.mjs
- package.json
- 可选 tests/*.mjs 或 scripts/test-core.mjs

**CI**:

- .github/workflows/check.yml（建议）

**部署**:

- vendor/（如选择本地化依赖）
- 404.html 或平台对应配置
- RELEASE.md
- README.md
- 可选部署配置文件

**文档**:

- CONTRIBUTING.md
- data-schema.md
- DATA_MAINTENANCE.md（建议新增）
- RELEASE.md（建议新增）

**可选 Backend Lite**:

- server/ 或 api/
- SQLite 数据文件（不要提交真实用户数据）
- .env.example

---

## 十、风险清单与应对

### [高] 学区 Polygon 无法获得官方精确边界

**影响**: 核心结果可信度受限。

**应对**:

- 不伪造"官方精确"；
- 明确 estimated / manualApproximation；
- 用公开文字范围进行交叉核验；
- 页面显示数据精度；
- 将"精确边界获取"列为后续数据升级任务。

### [高] 学期内拿不到完整 2026 数据

**影响**: Public Beta 可能出现年份混用。

**应对**:

- 页面明确显示每项数据年份；
- 不同年份数据不静默混合；
- 缺少数据则标 pending；
- 优先保证核心演示区域的高质量数据。

### [中] 天地图 POI / token / 网络异常

**影响**: 在线地址搜索不可用。

**应对**:

- 本地搜索 fallback；
- 明确错误提示；
- 超时；
- AbortController；
- 服务区域过滤；
- 核心页面不依赖在线 POI 才能打开。

### [中] 部署比预期复杂

**影响**: 第 8 周 Beta 延迟。

**应对**:

- 优先选择静态托管；
- 不在首次上线同时引入复杂后端；
- 第 7 周即开始准备部署；
- Backend Lite 不得阻塞静态站上线。

### [中] 自查规则需求膨胀

**影响**: 24h 不够。

**应对**:

- 只做真实政策能支持的有限规则；
- 不做概率；
- 不做复杂推理；
- 无法覆盖的情形输出"需人工核验"。

### [中] Backend Lite 范围失控

**影响**: 挤占核心开发。

**应对**:

- 严格 6h timebox；
- 仅 health + feedback；
- 不做账号；
- 不做管理后台；
- 超时直接降级。

### [低/中] 测试投入过多

**影响**: 挤占功能和数据时间。

**应对**:

- 不追求覆盖率；
- 只写高风险固定样例；
- 依靠 validator + 少量 core regression。

---

## 十一、砍项策略

### 【绝对不能砍】

1. P0-1 核心查询正确性
2. P0-2 数据可信化
3. P0-3 入学条件自查的最小可用版本
4. P0-4 Public Beta 首次上线
5. 移动端核心可用
6. check:release
7. 最低限度核心回归样例
8. README / 数据维护说明
9. 答辩材料
10. 最终 Release

### 【延期时优先砍】

**第一顺位**:

- P2-2 Backend Lite（6h）

**第二顺位**:

- P2-1 地图/加载视觉完善（8h）

**第三顺位**:

- 打印中的非核心页面；
- 额外浏览器兼容细节；
- 非核心动画。

**已经降级到未来、不进入本学期核心预算**:

- 搜索历史；
- 政策收藏；
- 全站搜索；
- 面包屑；
- 网站地图；
- "好差评"；
- 学区对比；
- 学位预警 Mock；
- 复杂适老版；
- 账号系统。

### 【不能用来赶工的方法】

- 用 fake/placeholder 数据冒充 verified；
- 把人工边界改名成"官方边界"；
- 为完成模拟器而输出虚假概率；
- 为展示"后端"临时做无法维护的大量 API；
- 为追求测试覆盖率拖延 Beta 上线。

---

## 十二、Release 流程建议

**开发阶段**:

1. 修改代码/数据；
2. npm run lint；
3. npm run validate:data；
4. 运行核心回归；
5. 提交 PR / 合并。

**Beta 发布**:

1. 冻结要发布的数据；
2. npm run check；
3. npm run check:release；
4. 本地 smoke test；
5. 部署；
6. 手机打开线上 URL；
7. 测试：首页、地址搜索、地图点击、双学段结果、政策、材料、自查；
8. 确认版本号/更新时间；
9. 写 release note。

**回滚**:

- 保留上一版本构建；
- 数据修改保持 Git 历史；
- 严重查询错误出现时优先回滚，不在线热修未验证数据。

---

## 十三、建议的版本节奏

| 版本 | 时间 | 内容 |
| --- | --- | --- |
| v0.8 | 第 4 周 | 核心查询修复；数据结构统一；validator / CI 初版 |
| v0.9 | 第 6 周 | 入学条件自查；政策/材料联动 |
| v0.9.5-beta | 第 8 周 | 移动端；搜索容错；第一次线上 Public Beta |
| v0.9.8 | 第 10 周 | 功能冻结；文档；回归；发布流程完整 |
| v1.0-beta | 第 12 周 | 稳定线上版本；答辩版本；数据与文档同步；后续工作有明确 backlog |

---

## 十四、远期路线图（本学期后）

### 【阶段 A：数据运营后台】

当真实运营需要频繁更新数据时，再引入：

- 管理员登录；
- 学校/政策/材料编辑；
- GeoJSON 上传或边界编辑；
- 数据审核；
- 发布/撤回；
- 数据版本；
- 修改审计；
- 一键生成静态发布数据。

此时数据库模型应根据业务领域设计，而不是"一个 JSON 文件对应一张表"。

### 【阶段 B：完整后端】

根据真实需求增加：

- REST API；
- 数据缓存；
- 监控；
- 日志；
- 权限；
- 反馈管理；
- 数据版本 API。

技术栈可再选: FastAPI、Express、其他团队熟悉方案。

### 【阶段 C：账号体系（有明确价值再做）】

只有出现这些真实需求时再考虑:

- 跨设备收藏；
- 跨设备历史；
- 用户申请进度；
- 个人材料状态；
- 个性化通知。

不要为了"网站看起来更完整"而先做登录注册。

### 【阶段 D：数据能力】

- 更精确官方学区数据；
- 多年度学区边界；
- 边界变化可视化；
- 更完整学校数据；
- 正式学位预警（必须有真实招生容量/人口依据）；
- 数据自动更新辅助工具；
- 人工审核工作流。

### 【阶段 E：更成熟的生产运维】

- 错误监控；
- 性能监控；
- 可用性监控；
- 自动部署；
- staging / production；
- CDN；
- 访问分析；
- 安全加固；
- 正式合规评估。

---

## 十五、项目完成定义（Definition of Done）

本学期项目不以"所有想法都实现"为完成标准。

满足以下条件即可认为 Smart Admission Public Beta v1.0 达成：

1. 用户能通过公网 HTTPS 打开网站；
2. 地址查询可以正确同时得到小学和初中归属；
3. 查询逻辑不受地图显示状态影响；
4. 关键数据有来源、年份和状态；
5. 正式发布检查不会放过明显占位数据；
6. 入学自查输出基于规则和政策，而不是虚假概率；
7. 手机端可以完成全部核心流程；
8. 天地图 POI 异常有降级；
9. 至少有一组自动核心业务回归；
10. 文档说明如何更新数据和重新发布；
11. 无已知 Blocker / 核心 Critical Bug；
12. 有 v1.0-beta 在线版本和可复现本地版本；
13. 答辩展示内容与真实实现完全一致。

达到以上标准后，"收藏 / 搜索历史 / 好差评 / 学区对比 / 学位预警 / 账号系统"均可以留到未来版本，不影响本学期目标成功。

---

## 十六、最终一句话目标

不是把 Smart Admission 做成"功能最多的课程 Demo"，而是把最重要的功能做正确、把数据做可信、把项目真正部署出去：

> **从「能演示」推进到「真的有人可以打开并使用的 Public Beta」。**
